# Never let local credentials into the build context.
.env
.env.*
!.env.example

**/bin/
**/obj/
.git/
project-os/
tools/
infra/
# libs/ and spec/ are NOT excluded: the API depends on the generated contracts,
# and the container build fails without them. (T-0001 left these excluded with a
# note to remove them here; this is that removal.)
