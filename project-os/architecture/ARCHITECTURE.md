# Architecture Overview

> ⚠ **Populated during [`bootstrap-project`](../skills/bootstrap-project/SKILL.md)** and maintained by the Software Architect persona. This is the current-state map of the system; the *history and rationale* of decisions lives in [`adr/`](adr/README.md). When this overview and an Accepted ADR disagree, the ADR wins and this file has a bug — fix it.

Keep this file short enough to actually read: a map, not a specification. Detail belongs in ADRs, standards, or the code.

## System context

*What the system is, its users, and the external systems it talks to. A diagram (Mermaid) plus a paragraph is usually enough.*

*TBD `[open]`*

## System boundaries and major components

*The main components/services/modules, their responsibilities, and the boundaries agents must not blur. This is the section implementers consult to know where new code belongs.*

*TBD `[open]`*

## Data

*Principal data stores, ownership of data by component, flow of data through the system, retention/PII notes.*

*TBD `[open]`*

## Cross-cutting concerns

*How the system handles: configuration, errors, logging/observability, authn/authz, background work. One short subsection each, linking ADRs.*

*TBD `[open]`*

## Technical constraints

*Constraints implementers must respect (from `PROJECT.md` §4/§5 and Accepted ADRs), gathered here for convenience. `PROJECT.md` and ADRs remain authoritative.*

*TBD `[open]`*

## Active architectural concerns

*Known debt, risks, and open architectural questions, each linked to a ticket or ADR. Not a wishlist.*

*TBD `[open]`*
